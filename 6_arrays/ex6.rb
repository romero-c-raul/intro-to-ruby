# names = ['bob', 'joe', 'susan', 'margaret']
# names['margaret'] = 'jody'

# it seems that the inside of the brackets expected an integer and not a string. Maybe this works for hashes???????

names = ['bob', 'joe', 'susan', 'margaret']
names[-1] = 'jody'

puts names